rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /bulletins/{bulletinId} {

      // Students can read active content. Unpublished resources stay visible only to staff.
      allow read: if isActiveAdvisor(request.auth)
        || (resource.data.isActive == true
          && (!('type' in resource.data)
            || resource.data.type != 'resource'
            || !('isPublished' in resource.data)
            || resource.data.isPublished == true));

      // Only advisors still on the advisor list can create content
      allow create: if isActiveAdvisor(request.auth)
        && validateBulletinData(request.resource.data)
        && request.resource.data.postedBy == getUsername(request.auth.token.email);

      // Any active advisor can update ANY post or resource. Content
      // permissions are equal for every advisor (Sep 2026) — the only thing
      // an admin can do that a plain advisor cannot is add and remove
      // people. The immutable-field checks below keep authorship and
      // timestamps honest no matter who does the editing.
      //
      // History: this used to be two separate `allow update` statements — a
      // cheap "reorder fast-path" declared before an expensive full-
      // validation rule, on the theory that Firestore would grant on the
      // first and skip the second. Firestore evaluates every `allow`
      // statement for an operation (and every branch of a combined `||`)
      // regardless of ordering or short-circuiting — verified empirically
      // against the local emulator — so having two large statements/branches
      // in play at once (previously: the reorder checks plus the full
      // validateBulletinData() call) blew the shared ~1000-expression budget
      // on an ordinary content edit (e.g. changing a resource's description)
      // and denied it with a bare "permission-denied" unrelated to actual
      // permissions. The fix here is a single, smaller rule rather than
      // clever branch ordering.
      allow update: if isActiveAdvisor(request.auth)
        && validateBulletinData(request.resource.data)
        && (!('postedBy' in request.resource.data) || request.resource.data.postedBy == resource.data.postedBy)
        && (!('datePosted' in request.resource.data) || request.resource.data.datePosted == resource.data.datePosted)
        && (!('createdAt' in request.resource.data) || !('createdAt' in resource.data) || request.resource.data.createdAt == resource.data.createdAt);

      // Any active advisor can permanently delete any bulletin — equal
      // privilege (Sep 2026). This also makes the 133 legacy posts left over
      // from the retired shared logins (postedBy 'admin' / 'advisors', owned
      // by nobody) editable and removable by the whole team.
      allow delete: if isActiveAdvisor(request.auth);
    }

    match /resourceLogos/{bulletinId} {
      // Resource logos live here instead of inline on the bulletin doc so the
      // board's single always-on query doesn't carry base64 image data for
      // every resource on every load (see src/resource-logos.js). Logos
      // aren't sensitive — same public-read posture as the resource cards
      // they belong to.
      allow read: if true;

      allow create, update: if isActiveAdvisor(request.auth)
        && request.resource.data.keys().hasOnly(['dataUrl', 'updatedAt'])
        && request.resource.data.dataUrl is string
        && request.resource.data.dataUrl.size() > 0
        && request.resource.data.dataUrl.size() < 6000000;

      allow delete: if isActiveAdvisor(request.auth);
    }

    match /users/{username} {
      // Users can read and write their own user document
      allow read, write: if isAdvisor(request.auth)
        && username == getUsername(request.auth.token.email);

      // Admins manage the roster, so they can read/write any profile doc.
      allow read, write: if isAdminAdvisor(request.auth);
    }

    match /advisors/{username} {
      // Advisors can read their own directory entry (google-auth.js uses this
      // to verify portal access at sign-in), and any active advisor can read
      // the whole list. For everyone else the read is denied, which the
      // client treats the same as "not an advisor".
      allow read: if isActiveAdvisor(request.auth)
        || (isAdvisor(request.auth) && username == getUsername(request.auth.token.email));

      // Adding and removing people is the ONE privilege an admin has beyond
      // a normal advisor. Gated on the advisors/{username}.isAdmin flag, so
      // the Admin toggle on the portal's Advisors tab is now the real switch
      // — promoting someone no longer needs a code change and a rules deploy.
      allow write: if isAdminAdvisor(request.auth);
    }

    match /config/studentDirectory {
      // The student-facing advisor directory (names, titles, contact emails).
      // Published from the admin Advisors tab; the student site falls back to
      // the static list in src/advisor-directory.js when this doc is absent.
      allow read: if true;
      allow write: if isAdminAdvisor(request.auth)
        && request.resource.data.advisors is list
        && request.resource.data.advisors.size() <= 50;
    }

    match /analyticsEvents/{eventId} {
      // Engagement analytics have been removed to keep Firestore costs low.
      allow read, write: if false;
    }

    match /errors/{errorId} {
      // Public clients can append sanitized runtime errors for monitoring.
      allow create: if validateClientError(request.resource.data);

      // Active advisors can review error logs in Firebase Console.
      allow read: if isActiveAdvisor(request.auth);

      // Error logs are append-only from the public client.
      allow update, delete: if false;
    }

    function getUsername(email) {
      return email.split('@')[0];
    }

    // email_verified blocks self-registered email/password accounts that
    // claim an @ebhcs.org address without proving it. Google Workspace
    // sign-in (the only supported login) always sets it to true.
    function isAdvisor(authInfo) {
      return authInfo != null
        && authInfo.token.email.matches('.*@ebhcs\\.org')
        && authInfo.token.email_verified == true;
    }

    // An advisor is "active" only while their advisors/{username} doc exists.
    // Removing an advisor in the portal (Advisors tab -> Remove) therefore
    // revokes their access immediately, even if their Firebase Auth login
    // still exists. Every active advisor has the same content permissions:
    // create, edit and delete any post or resource.
    function isActiveAdvisor(authInfo) {
      return isAdvisor(authInfo)
        && (isOwnerAdmin(authInfo.token.email)
          || exists(/databases/$(database)/documents/advisors/$(getUsername(authInfo.token.email))));
    }

    // An admin is any advisor whose advisors/{username} doc has isAdmin: true.
    // The ONLY thing this grants over a plain advisor is managing the roster
    // (advisors/*, config/studentDirectory, users/*). Read as a document get
    // rather than a hardcoded list so the portal's Admin toggle is the source
    // of truth; the exists() guard keeps a missing doc from erroring the rule.
    function isAdminAdvisor(authInfo) {
      return isAdvisor(authInfo)
        && (isOwnerAdmin(authInfo.token.email)
          || (exists(/databases/$(database)/documents/advisors/$(getUsername(authInfo.token.email)))
            && get(/databases/$(database)/documents/advisors/$(getUsername(authInfo.token.email)))
                 .data.get('isAdmin', false) == true));
    }

    // Break-glass only: the site owner always counts as an active advisor and
    // an admin, so clearing the last isAdmin flag (or deleting your own
    // advisor doc) can't permanently orphan the roster. Not an extra
    // day-to-day privilege — identical rights to any other admin.
    // Kept in sync with OWNER_ADMIN_EMAILS in src/admin-roles.js by
    // scripts/check-admin-emails-sync.mjs (prebuild).
    function isOwnerAdmin(email) {
      return email == 'mcreed@ebhcs.org';
    }

    // actionLinks / hoursRows are bounded by type and length only.
    //
    // These used to validate every item individually, unrolled per index
    // (5 action links x ~13 expressions, 7 hours rows x ~4). Firestore
    // evaluates every `allow` statement for an operation regardless of
    // short-circuiting, so on a single resource write validateBulletinData()
    // ran three times over (create @ L15, update @ L38, create again) and
    // those ~100 unrolled expressions tripled into the shared ~1000-expression
    // ceiling: a resource carrying the maximum 5 links and 7 hours rows was
    // rejected with a bare "permission-denied" that had nothing to do with
    // permissions. Verified against the emulator in
    // tests/rules/firestore-rules.test.mjs, which fails if this regresses.
    //
    // Same reasoning as the display-only fields dropped from
    // validateBulletinData() below: the labels and urls inside these lists are
    // rendered text the client already builds with the right shape (see
    // buildBulletinObject in src/firebase-admin.js, and the protocol allowlist
    // in src/resource-action-links.js with its unit tests). A malformed label
    // is a data-quality problem, not a security one — every write here still
    // requires an authenticated active advisor, and the list length caps still
    // bound how much can be written.
    function validActionLinks(links) {
      return links is list && links.size() <= 5;
    }

    function validHoursRows(rows) {
      return rows is list && rows.size() <= 7;
    }

    function isDocumentResource(data) {
      return ('resourceKind' in data) && data.resourceKind == 'document';
    }

    function validResourceContact(data) {
      return isDocumentResource(data)
        ? (
          (('pdfUrl' in data) && data.pdfUrl is string && data.pdfUrl.size() > 0)
          || (('url' in data) && data.url is string && data.url.size() > 0)
          || (('actionLinks' in data) && validActionLinks(data.actionLinks) && data.actionLinks.size() > 0)
        )
        : (('url' in data) && data.url is string && data.url.size() > 0);
    }

    function validateBulletinData(data) {
      let hasCommonRequired = data.keys().hasAll(['type', 'advisorName', 'postedBy', 'isActive'])
        && data.type is string && data.type in ['post', 'resource']
        && data.advisorName is string && data.advisorName.size() > 0
        && data.postedBy is string && data.postedBy.size() > 0
        && data.isActive is bool;

      // Trimmed to only the fields that gate an authorization or visibility
      // decision elsewhere (isPublished/isPinned/hideFromMainFeed feed the
      // read rule above; resourceKind/actionLinks/resourceOrder/eventDates/
      // lastVerified bound size or shape to prevent abuse). Every other
      // field here (description, hours, address, phone, url, highlights,
      // dates/times, images, etc.) is display-only text the client already
      // constructs with the right shape (see buildBulletinObject in
      // src/firebase-admin.js) — type-checking each one individually added
      // ~35 evaluated expressions per write and, combined with the rest of
      // this rule, was blowing Firestore's shared ~1000-expression budget
      // and denying ordinary edits with a bare "permission-denied" that had
      // nothing to do with actual permissions. A malformed value in one of
      // the dropped fields is a rendering/data-quality issue, not a security
      // one — every write here still requires an authenticated active
      // advisor.
      let validOptionalFields =
        (!('eventDates' in data) || (data.eventDates is list && data.eventDates.size() <= 20))
        && (!('hasResourceLogo' in data) || data.hasResourceLogo is bool)
        && (!('isPublished' in data) || data.isPublished is bool)
        && (!('isPinned' in data) || data.isPinned is bool)
        && (!('hideFromMainFeed' in data) || data.hideFromMainFeed is bool)
        && (!('lastVerified' in data) || (data.lastVerified is string && data.lastVerified.size() <= 20))
        && (!('resourceKind' in data) || data.resourceKind in ['organization', 'document'])
        && (!('actionLinks' in data) || validActionLinks(data.actionLinks))
        && (!('hoursRows' in data) || validHoursRows(data.hoursRows))
        && (!('resourceOrder' in data) || data.resourceOrder == null
          || ((data.resourceOrder is int || data.resourceOrder is float)
            && data.resourceOrder >= 0 && data.resourceOrder <= 100000));

      let validPost = data.type == 'post'
        && data.keys().hasAll(['title', 'category'])
        && data.title is string && data.title.size() > 0 && data.title.size() <= 200
        && data.category is string && data.category in ['job', 'training', 'college', 'career-fair', 'announcement', 'resource', 'immigration', 'housing', 'health', 'food', 'esol', 'money', 'childcare'];

      let validResource = data.type == 'resource'
        && data.keys().hasAll(['title', 'titleEn', 'category', 'resourceCategory', 'isPublished'])
        && data.title is string && data.title.size() > 0 && data.title.size() <= 200
        && data.titleEn is string && data.titleEn.size() > 0 && data.titleEn.size() <= 200
        && (!('titleEs' in data) || data.titleEs.size() <= 200)
        && data.category == 'resource'
        // resourceCategory whitelist — mirrors AUTHORABLE_RESOURCE_CATEGORIES
        // in src/resource-categories.js. scripts/check-resource-categories-sync.mjs
        // (run in prebuild) fails the build if the two lists drift apart.
        && data.resourceCategory in ['immigration', 'jobs', 'housing', 'health', 'food', 'family', 'family-community', 'hse', 'college', 'legal-aid', 'money', 'esol', 'general', 'consulates']
        && (!('url' in data) || data.url is string)
        && validResourceContact(data);

      return hasCommonRequired && validOptionalFields && (validPost || validResource);
    }

    function validateClientError(data) {
      return data.keys().hasAll(['type', 'message', 'source', 'page', 'createdAt', 'dayKey'])
        && data.keys().hasOnly(['type', 'message', 'stack', 'filename', 'lineno', 'colno', 'source', 'page', 'userAgent', 'createdAt', 'dayKey'])
        && data.type is string
        && data.type in ['error', 'unhandledrejection']
        && data.message is string
        && data.message.size() > 0
        && data.message.size() <= 500
        && (!('stack' in data) || (data.stack is string && data.stack.size() <= 2000))
        && (!('filename' in data) || (data.filename is string && data.filename.size() <= 200))
        && (!('lineno' in data) || data.lineno == null || data.lineno is int)
        && (!('colno' in data) || data.colno == null || data.colno is int)
        && data.source is string
        && data.source in ['student', 'admin']
        && data.page is string
        && data.page.size() <= 200
        && (!('userAgent' in data) || (data.userAgent is string && data.userAgent.size() <= 300))
        && data.createdAt is timestamp
        && data.dayKey is string
        && data.dayKey.matches('^\\d{4}-\\d{2}-\\d{2}$');
    }
  }
}
